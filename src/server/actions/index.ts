"use server";

/**
 * Server Actions — Database Mutations
 *
 * All write operations to Supabase go here.
 * These run on the local Next.js standalone server inside Electron,
 * so they work fully offline (mutations are queued if Supabase is unreachable).
 *
 * Examples to implement in Phase 3:
 * - checkout(cart, paymentMethod) — create an order from the current cart
 * - addProduct(product) — add a new product to inventory
 * - updateProduct(id, updates) — modify product details
 * - recordCreditPayment(customerId, amount) — settle utang
 */
