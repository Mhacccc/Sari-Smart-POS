"use server";

/**
 * Server Queries — Database Reads
 *
 * All read operations from Supabase go here.
 * Called from Server Components or via TanStack Query on the client.
 *
 * Examples to implement in Phase 3:
 * - getProducts() — fetch all active products
 * - getProductByBarcode(barcode) — lookup for scanner
 * - getSalesHistory(dateRange) — admin dashboard data
 * - getCustomerCredit(customerId) — utang balance
 */
