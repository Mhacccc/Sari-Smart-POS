/**
 * Supabase-generated types placeholder.
 *
 * Run `npx supabase gen types typescript --project-id <your-project-id> > src/types/database.types.ts`
 * to generate real types from your Supabase schema.
 *
 * For now, this exports an empty Database type so imports don't break.
 */

export interface Database {
    public: {
        Tables: Record<string, never>;
        Views: Record<string, never>;
        Functions: Record<string, never>;
        Enums: Record<string, never>;
    };
}
