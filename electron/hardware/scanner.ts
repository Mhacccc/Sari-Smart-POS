/**
 * Barcode Scanner Listener
 *
 * Listens for HID barcode scanner input (most USB scanners act as keyboards)
 * and emits scanned barcodes to the renderer via IPC.
 *
 * Will be implemented in Phase 6.
 */

// import { BrowserWindow } from "electron";

// export function startBarcodeListener(mainWindow: BrowserWindow) {
//   // Most USB barcode scanners emulate keyboard input.
//   // This can be handled via keydown listeners in the renderer.
//   // If raw HID is needed, use the `node-hid` package.
// }
